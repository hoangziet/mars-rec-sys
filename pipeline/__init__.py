"""
pipeline/
=========
Shared building blocks for training, evaluation, and data loading.

Submodules:
    loaders  — dataset classes and DataLoader factories
    metrics  — full-sort evaluation functions and metric helpers
    builder  — model, criterion, eval-fn, and train-loader factories
"""
