# configs/model/bprmf.yaml
name: bprmf
model_kwargs:
  emb_dim: 64
train_kwargs:
  batch_size: 256
  epochs: 50
  lr: 1e-3
  weight_decay: 1e-4
  beta2: 0.98
  reg_lambda: 0.0
  max_len: 50
  gradient_clip: 5.0
  early_stop_patience: 10
  early_stop_min_delta: 1e-4
  confidence_alpha: 0.0
