# configs/model/bprmf.yaml
name: bprmf
model_kwargs:
  emb_dim: 64
train_kwargs:
  batch_size: 1024
  epochs: 50
  lr: 1e-3
  reg_lambda: 1e-4
  max_len: 50
  gradient_clip: 0
  early_stop_patience: 10
  early_stop_min_delta: 1e-4
  confidence_alpha: 0.0
